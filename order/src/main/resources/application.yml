spring:
  profiles:
    active: win
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: yongmall-order

server:
  port: 9000
